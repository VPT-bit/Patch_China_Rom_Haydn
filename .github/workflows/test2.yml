name: test2
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout
          uses: actions/checkout@main
        - name: test
          run: |
              sudo apt update -y
              sudo apt upgrade -y
              sudo apt-get install -y axel curl wget
              curl https://drive.google.com/uc?id=1EYX6ByaVPyHzex-6e0uyGDkX4XG_Qs2T&export=download
              
